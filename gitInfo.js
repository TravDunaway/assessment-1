
//////////////////PROBLEM 1////////////////////
/*
    Create a variable called 'gitDefinition'.
    It should be a string containing your best definition of what Git is.
*/

let gitDefinition = 'A git is a version control system, essentially i look at it as a way to save code, look at iterations of said code, all can be done with a VCS either individually or as a collaboration.'

//////////////////PROBLEM 2////////////////////
/*
    Create a variable called 'gitHubDefinition'.  
    It should be a string containing your best definition of what GitHub is.
*/

let gitHubDefinition = 'github is Microsofts VCS, its a website that allows us a location to store our git repositories.'

/////////////////PROBLEM 3////////////////////
/*
    Create a variable called 'gitInitDefinition'.  
    It should be a string containing your best definition of what 'git init' does.
*/

let gitInitDefinition = 'Once youve made any changes to a file, you can create a local-repository, also in order for that git to be located someplace else, also this repackages the git for later sharing'
//////////////////PROBLEM 4////////////////////
/*
    Create a variable called 'gitCloneDefinition'.  
    It should be a string containing your best definition of what 'git clone' does.
*/

let gitCloneDefinition = 'git clone is a command that allows us to create a copy of a stored-repo on a VCS, like that of github'

//////////////////PROBLEM 5////////////////////
/*
    Create a variable called 'gitStatusDefinition'.  
    It should be a string containing your best definition of what 'git status' does.
*/

let gitStatusDefinition = 'Git status command will tell you the current status of the git file, if you've made changes, how many changes, if youve saved it, your social security etc etc'

//////////////////PROBLEM 6////////////////////
/*
    Create a variable called 'gitAddDefinition'.  
    It should be a string containing your best definition of what 'git add' does.
l
    Create another variable called 'gitAddCode'.  
    It should be a string containing the code to add all files.
*/

//CODE HERE
let gitAddDefinition = 'add some files to be tracked, the git add command specifies what files are to be repacked for sharing, its like sending a package in the mail, git add is the "adding stuff to that package" part of the shipping process'

let gitAddCode = 'I think what im being asked for is git add .' (git add .)
//////////////////PROBLEM 7////////////////////
/*
    Create a variable called 'gitCommitDefinition'.  
    It should be a string containing your best definition of what 'git commit' does.

    Create a variable called 'gitCommitCode'.  
    It should be a string containing the code to commit using the message "initial commit".
*/

//CODE HERE
let gitCommitDefinition = 'this is the stage where you've confirmed you've got everything you want to send, its like a snapshot of the contents of the repo, also youre able to define whats in the repo during this command too.'

let gitCommitCode = git commit -m "initial commit" (i've seen it written git commit -am "initial commit")

//////////////////PROBLEM 8////////////////////
/*
    Create a variable called 'gitPushDefinition'.  
    It should be a string containing your best definition of what 'git push' does.
*/

//CODE HERE

let gitPushDefinition = 'the git push will actually send everything youve put in the git to the place where you are sending it, i.e. github.com or someother vcs.' Essentially, going back to my sending a package analogy, git push is the actual sending of the package part of the process 